<div class="diet container-fluid">
    <section class="dietChart">
        <div>
            <canvas id="myChart" width="50" height="50"></canvas>
            <script src="https://cdn.jsdelivr.net/npm/chart.js@2.9.4/dist/Chart.min.js"></script>
        </div>
    </section>
    <div class="dietEntries">
        {{!-- {{insert diet data here}} --}}
    </div>
</div>
{{#each foodQuantity as |food|}}
    {{log this}}
    <div>this is the day{{food.day}}</div>
{{/each}}


<script>
    
let labels = [];
{{!-- let time = []; --}}
let values = [];

{{!-- this is not setup yet --}}
{{#each sortedFoodQuantity}}
    {{!-- {{log this}} --}}
    labels.push("{{day}}")
    {{!-- time.push("{{time}}") --}}
    values.push("{{quantity}}")
{{/each}}

console.log(values);

const ctx = document.getElementById('myChart');
const myChart = new Chart(ctx, {
    type: 'bar',
    data: {
        {{!-- labels is labels:labels --}}
        labels,
        datasets: [{
            label: 'Try to remove?',
            data: values,
            backgroundColor: [
                'rgba(255, 99, 132, 0.2)',
                'rgba(54, 162, 235, 0.2)',
                'rgba(255, 206, 86, 0.2)',
                'rgba(75, 192, 192, 0.2)',
                'rgba(153, 102, 255, 0.2)',
                'rgba(255, 159, 64, 0.2)'
            ],
            borderColor: [
                'rgba(255, 99, 132, 1)',
                'rgba(54, 162, 235, 1)',
                'rgba(255, 206, 86, 1)',
                'rgba(75, 192, 192, 1)',
                'rgba(153, 102, 255, 1)',
                'rgba(255, 159, 64, 1)'
            ],
            borderWidth: 1
        }]
    },
    options: {
        scales: {
            y: {
                beginAtZero: true,
            }
        }
    }
});
</script>